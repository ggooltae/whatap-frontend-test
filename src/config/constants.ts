export const TIME = {
  SECOND: 1000,
  MINUTE: 1000 * 60,
  HOUR: 1000 * 60 * 60,
  INTERVAL_DELAY: 5000,
};

export const SIZE = {
  PROJECT_DATA_SIZE_LIMIT: (10 * TIME.MINUTE) / (5 * TIME.SECOND),
};
